spring:
  application:
    name: content   # Eureka service name
  datasource:
    url: jdbc:mysql://localhost:3306/db9
    username: root
    password: SreeShiv@0727
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

server:
  port: 8084

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
